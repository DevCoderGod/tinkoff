{
  "name": "tinkoff_sdk",
  "version": "0.1.0",
  "author": "devcodergod",
  "description": "",
  "scripts": {
	"getGRPC": "npm run getGRPC --prefix server",
    "getProto": "npm run getProto --prefix shared",
    "dev:server": "npm run start --prefix server",
    "prod:server": "npm run prod --prefix server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "adm-zip": "^0.5.9",
    "concurrently": "^7.4.0"
  },
  "devDependencies": {
    "@protobuf-ts/plugin": "^2.8.2",
    "@protobuf-ts/runtime": "^2.8.2",
    "typescript": "^4.8.4"
  }
}
